# Python
__pycache__/
*.pyc
.venv/

# MLflow local tracking store (regenerated by `dvc repro` / training stage)
mlruns/
mlflow.db

# DVC-managed pipeline outputs (tracked via dvc.lock, not committed directly)
/data/processed/
